// import { screen } from '@testing-library/react';
// import render from './render';
// import { ProductsList } from '@/pages';

it('페이지 로드시 10개의 상품이 보여진다', async () => {
  // render(<ProductsList />);
  // expect(await screen.findByPlaceholderText('상품을 입력해주세요')).toBeInTheDocument();
});
